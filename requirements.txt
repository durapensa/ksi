# No Python dependencies required - uses only standard library

# System dependencies (install with brew on macOS):
# - socat: for Unix socket communication
# - claude: the Claude CLI (see https://claude.ai/code)

# To install system dependencies on macOS:
# brew install socat