"""
KSI Built-in Plugins

This package contains the built-in plugins that provide core functionality
for the KSI daemon. Each subdirectory represents a plugin category:

- core: Essential plugins (command compatibility, state management)
- transport: Connection transports (Unix sockets, SocketIO, HTTP)
- completion: LLM completion providers (Claude CLI, mock)
- agent: Agent orchestration and management
- messaging: Event routing and pub/sub
"""