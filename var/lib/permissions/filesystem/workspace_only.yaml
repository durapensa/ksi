# Workspace-only filesystem permissions
name: workspace_only_filesystem
type: permission_fragment
version: 1.0.0
description: Restricts access to agent's own workspace

filesystem:
  sandbox_root: ./workspace
  read_paths:
    - ./workspace
  write_paths:
    - ./workspace
  allow_symlinks: false